general

* use xmalloc
* handle other encodings
- TrueType collections
- CID keyed t42 fonts

both

* error handling (e. g. write error)
* validate lexical correctness of glyph names
* version vs. font_revision
* Weight (subfamily is wrong)
- XUID

afm

* CapHeight, XHeight, Ascender, Descender, StdHW, StdVW

t42

* PaintType (how to recognize 2, StrokeWidth)
* fix checksums (how?!)
* remove duplicat definition of .notdef in CharStrings
* detect missing tables
- embed full font (--full)
